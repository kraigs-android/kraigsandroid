<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.angrydoughnuts.android.alarmclock"
      android:versionCode="1" android:versionName="1.0.0" xmlns:android="http://schemas.android.com/apk/res/android">

  <application android:icon="@drawable/icon" android:label="@string/app_name">
    <activity android:name=".AlarmClockActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"></action>
        <category android:name="android.intent.category.LAUNCHER"></category>
      </intent-filter>
    </activity>
    <activity android:name="AlarmNotificationActivity" android:screenOrientation="portrait"></activity>
    <activity android:name="SettingsActivity"></activity>
    <service android:name="AlarmClockService"></service>
    <receiver android:name="AlarmBroadcastReceiver"></receiver>
  </application>

  <uses-sdk android:minSdkVersion="4" />
  <uses-permission android:name="android.permission.WAKE_LOCK"></uses-permission>
  <uses-permission android:name="android.permission.DISABLE_KEYGUARD"></uses-permission>
  <!-- TODO(cgallek): This is only for the exception handler.  Remove it eventually? -->
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>
</manifest> 
